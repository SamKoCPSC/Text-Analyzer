package ui.unused;

public class RepeatBotList {
    /*public ArrayList<RepeatBot> bots;

    public RepeatBotList() {
        bots = new ArrayList<>();
    }

    public void addBot(RepeatBot b) {
        if (!bots.contains(b)) {
            bots.add(b);
            b.addList(this);
        }
    }

    public void removeBot(RepeatBot b) {
        if (bots.contains(b)) {
            bots.remove(b);
            b.removeList(this);
        }
    }*/
}
